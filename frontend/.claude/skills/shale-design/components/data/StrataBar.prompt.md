Horizontal progress/meter bar with a sandstone well and faintly textured strata fill; use for strategy allocation, APEX buffer health, or TVL splits. Add `target` for an actual-vs-target drift tick.

```jsx
<StrataBar label="Aave V3" valueLabel="32% / 30%" value={32} target={30} tone="apex" />
<StrataBar label="APEX Buffer" valueLabel="18.4%" value={18.4} tone="warning" height={12} />
```
