Three-dot risk indicator keyed to a tier; sits in tier cards and tables. Lit dots take the tier color.

```jsx
<RiskMeter level={1} />   {/* CORE — 1 green dot, "Low Risk" */}
<RiskMeter level={3} />   {/* APEX — 3 rust dots, "High Risk" */}
```
